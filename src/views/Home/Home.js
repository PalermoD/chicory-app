import React from 'react';
import RetailersForm from '../../containers/RetailersForm'

const HomeView = () => {
  // Declare a new state variable, which we'll call "count"
  return (
    <RetailersForm />
  );
}

export default HomeView;
